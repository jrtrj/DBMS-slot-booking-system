spring.application.name=slot-booking-system

spring.datasource.url=jdbc:mariadb://localhost:3306/college_slots
spring.datasource.username=jry
spring.datasource.password=applepapple
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Initialize the database only when using an embedded database (like H2)
# For production with a persistent database like MariaDB, this should be 'never' or 'embedded'
spring.sql.init.mode=embedded

# Disable Hibernate's automatic schema generation to avoid conflicts
spring.jpa.hibernate.ddl-auto=none
